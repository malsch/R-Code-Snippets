# allgemeine Befehle
dist(x) # berechnet Distanzen von Matrizen/Vektoren
(0:9)[factor] # greift auf die Levels eines Factors zu um as.numeric(factor) sinnvoll zu gestalten

# Packages zum Reinlesen
library(CORElearn)
library(caret)
library(FNN) # von kaggle empfohlen als schneller kNN Algorithmus
library(kernlab) # SVM-Funktion ksvm
library(ada) # Stochastic Boosting
library(rattle) # GUI for data mining

# Notizen und Quellen für weitere interessante Information
# http://musicdatascience.com/the-winners/
# Hackaton - especially 2nd and 3rd price winner code should be tried out
